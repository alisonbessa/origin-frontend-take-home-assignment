import * as React from 'react';
import { mount } from 'enzyme';
import ReachByDate from '../index';

const wrapper = mount(<ReachByDate />);

describe('TotalAmount rendering', () => {
// TODO: Test component
});
