import * as React from 'react';
import { mount } from 'enzyme';
import TotalAmount from '../index';

const wrapper = mount(<TotalAmount />);

describe('TotalAmount rendering', () => {
// TODO: Test component
});
